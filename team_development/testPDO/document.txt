間違ってたらごめんなさい。

・PDOの初期設定をする。ここが厄介である。自分はセキュリティソフトのAvastが悪さをしてめちゃくちゃ手間取った。
・index.phpの3~5行目にユーザー名などを入力する。
・MySQLを開き、ログインする。
・test.sqlに記載したSQL文を実行してデータベースを作成する。テーブルとサンプルデータはページ読み込み時に自動的に作られるので、ここで作らなくてよい。
・VScodeのターミナルに「php -S localhost:〇〇〇〇」と入力する。
・ブラウザのアドレスバーで「localhost:〇〇〇〇」と入力する。
・画面に「接続に成功しました」と「さーて、来週の〇〇さんは？」と表示されれば成功である。〇〇にはランダムでチームメンバーの名前が入る。
